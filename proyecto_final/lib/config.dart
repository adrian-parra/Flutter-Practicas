import 'package:flutter/material.dart';

class AppConfig {
  static const colorPrincipal = Color(0xFF00A6A6);
  static const colorSecundario = Color(0xFFFFFFFF);
  static const colorFondo = Color(0xFFF2F2F2);
  static const colorDescripcion = Color(0xFF757575);
  static const colorExtra = Color(0xFF2B2A2A);

  static const colorSuccess = Color(0xFF51ae57);
  static const colorDanger = Color(0xFFff0000);
  static const colorWarning = Color(0xFFf57c00);
  static const colorInfo = Color(0xFF3498db);

  static const marginValue = 16.0;
  static const paddingValue = 16.0;

  static const sizeTitulo = 24.0;
  static const sizeSubtitulo = 20.0;
  static const sizeDescripcion = 14.0;

  static const gap = 10.0;

  static const letterSpacingValue = 1.3;

  static const apiUrl = 'http://localhost:5220/api/';
  static const apiHost = 'http://localhost:5220/';

}